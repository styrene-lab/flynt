name: Deploy codex.styrene.io

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - '.github/workflows/deploy-site.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Cloudflare Pages
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install
        run: cd site && npm install

      - name: Build
        run: cd site && npx astro build

      - name: Deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          npx wrangler@latest pages project create codex-styrene-io --production-branch main 2>&1 || true
          npx wrangler@latest pages deploy site/dist --project-name codex-styrene-io --commit-dirty=true

      - name: Attach custom domain
        if: github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          curl -s -X POST \
            "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/pages/projects/codex-styrene-io/domains" \
            -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
            -H "Content-Type: application/json" \
            -d '{"name":"codex.styrene.io"}' | python3 -c "
          import json,sys
          r=json.load(sys.stdin)
          errs=[e for e in r.get('errors',[]) if e.get('code') not in (8000018,8000019)]
          sys.exit(1 if errs else 0)
          "

      - name: Upsert DNS
        if: github.ref == 'refs/heads/main'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          python3 - <<'EOF'
          import json, os, sys, urllib.request

          token = os.environ["CLOUDFLARE_API_TOKEN"]
          headers = {"Authorization": f"Bearer {token}", "Content-Type": "application/json"}

          def cf(method, path, body=None):
              url = f"https://api.cloudflare.com/client/v4{path}"
              data = json.dumps(body).encode() if body else None
              req = urllib.request.Request(url, data=data, headers=headers, method=method)
              with urllib.request.urlopen(req) as r:
                  return json.load(r)

          zones = cf("GET", "/zones?name=styrene.io")
          zone_id = zones["result"][0]["id"]

          existing = cf("GET", f"/zones/{zone_id}/dns_records?name=codex.styrene.io&per_page=100")
          for rec in existing["result"]:
              if rec["type"] in ("A", "AAAA", "CNAME"):
                  cf("DELETE", f"/zones/{zone_id}/dns_records/{rec['id']}")

          result = cf("POST", f"/zones/{zone_id}/dns_records", {
              "type": "CNAME",
              "name": "codex",
              "content": "codex-styrene-io.pages.dev",
              "proxied": True,
              "ttl": 1,
          })
          if result.get("success"):
              print("Created CNAME codex.styrene.io -> codex-styrene-io.pages.dev")
          else:
              print(f"ERROR: {result.get('errors')}", file=sys.stderr)
              sys.exit(1)
          EOF
